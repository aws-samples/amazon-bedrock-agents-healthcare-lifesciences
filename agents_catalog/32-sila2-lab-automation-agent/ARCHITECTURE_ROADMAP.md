# アーキテクチャロードマップ - SiLA2 Lab Automation Agent

## 🎯 アーキテクチャ進化

### Current State (Phase 2 Complete) ✅
```
┌─────────────────┐    ┌──────────────────────────────────┐
│ Amazon Bedrock  │    │      Bedrock AgentCore          │
│ Claude 3.5      │◄───┤      + Strands Agent SDK        │
│ Sonnet v2       │    │      + AgentCore Gateway        │
└─────────────────┘    └──────────────┬───────────────────┘
                                      │
                       ┌──────────────▼───────────────┐
                       │   AgentCore Gateway Tools    │
                       │  ┌─────────────────────────┐ │
                       │  │  SiLA2-MCP Bridge      │ │
                       │  │  • Native Integration  │ │
                       │  │  • Auto Scaling        │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │
                       ┌──────────────▼───────────────┐
                       │    Simulated SiLA2 Tools    │
                       │  • list_available_devices() │
                       │  • get_device_status()      │
                       │  • execute_device_command()  │
                       └──────────────────────────────┘
```

### Target State (Phase 4) 🎯
```
┌─────────────────┐    ┌──────────────────────────────────┐
│ Amazon Bedrock  │    │      Bedrock AgentCore          │
│ Claude 3.5      │◄───┤      + Strands Agent SDK        │
│ Sonnet v2       │    │      + AgentCore Gateway        │
└─────────────────┘    └──────────────┬───────────────────┘
                                      │ HTTP/REST
                       ┌──────────────▼───────────────┐
                       │   AgentCore Gateway Tools    │
                       │  ┌─────────────────────────┐ │
                       │  │  Simplified MCP Bridge │ │
                       │  │  • Basic Tool Routing  │ │
                       │  │  • Auth & Validation   │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │ HTTP/REST
                       ┌──────────────▼───────────────┐
                       │    API Gateway + Lambda      │
                       │  ┌─────────────────────────┐ │
                       │  │  SiLA2 Protocol Bridge │ │
                       │  │  • HTTP ↔ gRPC Convert│ │
                       │  │  • Device Abstraction │ │
                       │  │  • Multi-device Router │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │ gRPC
┌─────────────────────────────────────▼─────────────────────────────────┐
│                    SiLA2 Equipment Layer                              │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐   │
│  │ Tecan Fluent    │  │ API Gateway +   │  │ Other SiLA2         │   │
│  │ gRPC Server     │  │ Mock SiLA2      │  │ Equipment           │   │
│  │                 │  │ Devices (Lambda)│  │                     │   │
│  └─────────────────┘  └─────────────────┘  └─────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 フェーズ別アーキテクチャ進化

### Phase 1: MCP統合基盤 ✅ **完了**
```
Basic MCP Integration
┌─────────────────┐    ┌──────────────────────────────────┐
│ Amazon Bedrock  │    │      Bedrock AgentCore          │
│ Claude 3.5      │◄───┤      + Strands Agent SDK        │
│ Sonnet v2       │    │                                  │
└─────────────────┘    └──────────────┬───────────────────┘
                                      │
                       ┌──────────────▼───────────────┐
                       │         MCP Layer            │ ← NEW
                       │  ┌─────────────────────────┐ │
                       │  │    MCP Server (Local)   │ │
                       │  │  • SiLA2 MCP Tools     │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │
                       ┌──────────────▼───────────────┐
                       │    Basic SiLA2 Tools        │ ← ENHANCED
                       │  • device_status()          │
                       │  • execute_command()        │
                       └──────────────────────────────┘
```

**新規コンポーネント**:
- MCP Server (Local Python)
- SiLA2 MCP Tools
- Basic Device Simulation

### Phase 2: AgentCore Gateway統合 ✅ **完了**
```
AgentCore Gateway Integration
┌─────────────────┐    ┌──────────────────────────────────┐
│ Amazon Bedrock  │    │      Bedrock AgentCore          │
│ Claude 3.5      │◄───┤      + Strands Agent SDK        │
│ Sonnet v2       │    │      + AgentCore Gateway        │ ← NEW
└─────────────────┘    └──────────────┬───────────────────┘
                                      │
                       ┌──────────────▼───────────────┐
                       │   AgentCore Gateway Tools    │ ← NEW
                       │  ┌─────────────────────────┐ │
                       │  │  SiLA2-MCP Bridge      │ │
                       │  │  • Native Integration  │ │
                       │  │  • Managed Service     │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │
                       ┌──────────────▼───────────────┐
                       │    Enhanced SiLA2 Tools     │ ← ENHANCED
                       │  • list_available_devices() │
                       │  • get_device_status()      │
                       │  • execute_device_command()  │
                       │  • start_measurement()      │
                       │  • stop_measurement()       │
                       └──────────────────────────────┘
```

**新規コンポーネント**:
- AgentCore Gateway Service
- SiLA2-MCP Bridge (Native)
- Enhanced SiLA2 Tool Set
- Managed Scaling & Security

### Phase 3: SiLA2 Protocol実装 🚧 **次のフェーズ**
```
SiLA2 Unified API Gateway Implementation
┌─────────────────┐    ┌──────────────────────────────────┐
│ Amazon Bedrock  │    │      Bedrock AgentCore          │
│ Claude 3.5      │◄───┤      + Strands Agent SDK        │
│ Sonnet v2       │    │      + AgentCore Gateway        │
└─────────────────┘    └──────────────┬───────────────────┘
                                      │ Direct Lambda Invoke
                       ┌──────────────▼───────────────┐
                       │   AgentCore Gateway Tools    │
                       │  ┌─────────────────────────┐ │
                       │  │  MCP Tool Registry     │ │ ← SIMPLIFIED
                       │  │  • Lambda ARN Mapping  │ │
                       │  │  • Basic Auth          │ │
                       │  └─────────────────────────┘ │
                       └──────────────────────────────┘
                                      │ Direct Lambda Invoke
                       ┌──────────────▼───────────────┐
                       │    MCP-gRPC Bridge Lambda    │ ← NEW LAYER
                       │  ┌─────────────────────────┐ │
                       │  │  • MCP Tool Handler    │ │
                       │  │  • Device Router       │ │
                       │  │  • Mock/Real Switcher  │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │ gRPC over HTTPS
                       ┌──────────────▼───────────────┐
                       │    Device API Gateway        │ ← UNIFIED ENDPOINT
                       │  ┌─────────────────────────┐ │
                       │  │  • Mock/Real Routing   │ │
                       │  │  • Auth & Rate Limit   │ │
                       │  │  • Device Discovery    │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │ gRPC
┌─────────────────────────────────────▼─────────────────────────────────┐
│                    Mock Device Layer (Phase 3)                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐   │
│  │ Mock HPLC       │  │ Mock Centrifuge │  │ Mock Pipette        │   │
│  │ SiLA2 Device    │  │ SiLA2 Device    │  │ SiLA2 Device        │   │
│  │ (Lambda)        │  │ (Lambda)        │  │ (Lambda)            │   │
│  └─────────────────┘  └─────────────────┘  └─────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**新規コンポーネント**:
- **MCP-gRPC Bridge Lambda** (MCPとgRPCの統合処理)
- **Device API Gateway** (統一デバイスエンドポイント)
- **Mock Device Router** (モックデバイス専用ルーティング)
- **Mock SiLA2 Device Simulators** (HPLC, Centrifuge, Pipette)
- **gRPC Server functionality in Lambda** (Mock デバイス実装)
- **Device Registry Foundation** (Phase 4拡張用基盤)

## 📋 Phase 3 実装計画

### 実装ステップ

#### Step 1: Gateway Tools簡素化
- `gateway/sila2_gateway_tools_simplified.py`を使用
- SiLA2Client依存関係を削除
- HTTP-based通信に変更

#### Step 2: MCP-gRPC Bridge実装
- `mcp_grpc_bridge_lambda.py`でMCPツール処理実装
- デバイスルーティング機能追加
- Mock/Real切り替え機能実装

#### Step 3: Mock Device API Gateway実装
- `mock_device_api_gateway.yaml`でモック専用エンドポイント作成
- Mock SiLA2デバイスシミュレーター実装
- 基本認証・レート制限・監視機能追加

#### Step 4: 統合テスト
- `test_phase3.py`で全体テスト
- AgentCore Runtime → Gateway → MCP-gRPC Bridge → Mock Devices
- エンドツーエンドMockデバイス動作確認

### 実装優先度
1. **HIGH**: Gateway Tools簡素化（既存機能維持）
2. **HIGH**: MCP-gRPC Bridge基本機能
3. **HIGH**: Mock Device API Gateway実装
4. **MEDIUM**: Mock SiLA2デバイスシミュレーター
5. **LOW**: 実デバイス対応基盤（Phase 4で実装）

### 技術要件
- Python 3.10環境
- grpcio>=1.50.0, protobuf>=4.21.0
- AWS Lambda, API Gateway
- CloudFormation自動デプロイ
- **Mock device configuration** (シミュレーター設定)
- **gRPC over HTTPS support** (API Gateway統合)
- **Device registry foundation** (Phase 4拡張用)

### Phase 4: Real Equipment統合 ⏳
```
Real Equipment Integration (Mock + Real)
┌─────────────────┐    ┌──────────────────────────────────┐
│ Amazon Bedrock  │    │      Bedrock AgentCore          │
│ Claude 3.5      │◄───┤      + Strands Agent SDK        │
│ Sonnet v2       │    │      + AgentCore Gateway        │
└─────────────────┘    └──────────────┬───────────────────┘
                                      │ Direct Lambda Invoke
                       ┌──────────────▼───────────────┐
                       │   AgentCore Gateway Tools    │
                       │  ┌─────────────────────────┐ │
                       │  │  Enhanced MCP Bridge   │ │ ← ENHANCED
                       │  │  • Multi-device Support│ │
                       │  │  • Mock/Real Switching │ │
                       │  └─────────────────────────┘ │
                       └──────────────────────────────┘
                                      │ Direct Lambda Invoke
                       ┌──────────────▼───────────────┐
                       │  Enhanced MCP-gRPC Bridge   │ ← ENHANCED
                       │  ┌─────────────────────────┐ │
                       │  │  • Mock/Real Router    │ │
                       │  │  • Multi-vendor Support│ │
                       │  │  • Advanced Discovery  │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │ gRPC over HTTPS
                       ┌──────────────▼───────────────┐
                       │  Enhanced Device API Gateway │ ← ENHANCED
                       │  ┌─────────────────────────┐ │
                       │  │  • Mock/Real Routing   │ │
                       │  │  • VPC Integration     │ │
                       │  │  • mTLS Authentication │ │
                       │  └─────────────────────────┘ │
                       └──────────────┬───────────────┘
                                      │ gRPC
┌─────────────────────────────────────▼─────────────────────────────────┐
│                Mixed Device Layer (Mock + Real)                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐   │
│  │ Mock HPLC       │  │ Mock Centrifuge │  │ Mock Pipette        │   │
│  │ SiLA2 Device    │  │ SiLA2 Device    │  │ SiLA2 Device        │   │
│  │ (Lambda)        │  │ (Lambda)        │  │ (Lambda)            │   │
│  └─────────────────┘  └─────────────────┘  └─────────────────────┘   │
│                                                                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐   │
│  │ Real Tecan      │  │ Real HPLC       │  │ Other Real SiLA2    │   │
│  │ Fluent SiLA2    │  │ System SiLA2    │  │ Equipment           │   │
│  │ (Edge/Lab)      │  │ (Edge/Lab)      │  │ (Edge/Lab)          │   │
│  └─────────────────┘  └─────────────────┘  └─────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**新規コンポーネント**:
- **Enhanced MCP-gRPC Bridge** (Mock/Real統合処理)
- **Mock/Real Device Router** (動的デバイス切り替え)
- **VPC Integration** (エッジデバイス接続)
- **mTLS Authentication** (実デバイス認証)
- **Multi-vendor Device Support** (複数ベンダー対応)
- **Advanced Device Discovery** (自動デバイス検出)

---

## 🎯 Phase 3 vs Phase 4 比較

| 項目 | Phase 3 (Mock Only) | Phase 4 (Mock + Real) |
|------|---------------------|------------------------|
| デバイス | Mock SiLA2のみ | Mock + Real SiLA2 |
| 接続方式 | Lambda SiLA2 | Lambda + Edge SiLA2 |
| 認証 | 基本認証 | mTLS + 基本認証 |
| ネットワーク | パブリック | VPC + パブリック |
| 切り替え | 不要 | 環境変数制御 |
| 複雑度 | 低 | 中 |

## 📈 段階的移行戦略

1. **Phase 3**: Mock環境で機能検証・開発
2. **Phase 4**: 実デバイス段階的追加
3. **運用**: Mock/Real混在環境での柔軟運用─────────────────────────┐
│                        AWS Cloud Environment                        │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐    ┌──────────────────────────────────────────┐ │
│  │ Amazon Bedrock  │    │         Bedrock AgentCore                │ │
│  │ Claude 3.5      │◄───┤         + Enhanced Observability         │ │ ← NEW
│  │ Sonnet v2       │    │         + Multi-Agent Support            │ │ ← NEW
│  └─────────────────┘    └──────────────────┬───────────────────────┘ │
│                                            │                         │
│  ┌─────────────────────────────────────────▼─────────────────────┐   │
│  │                Gateway API Layer                            │   │
│  │  ┌─────────────────────────────────────────────────────────┐ │   │
│  │  │              Enhanced Gateway                       │ │   │
│  │  │  • Multi-device Management                         │ │   │ ← NEW
│  │  │  • Workflow Orchestration                          │ │   │ ← NEW
│  │  │  • Auto-failover & Load Balancing                  │ │   │ ← NEW
│  │  └─────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────┬─────────────────────┘   │
│                                            │                         │
│  ┌─────────────────────────────────────────▼─────────────────────┐   │
│  │                SiLA2 Protocol Layer                         │   │
│  │  ┌─────────────────────────────────────────────────────────┐ │   │
│  │  │            Production SiLA2 Server                 │ │   │
│  │  │  • Advanced Protocol Handler                       │ │   │ ← NEW
│  │  │  • Intelligent Device Manager                      │ │   │ ← NEW
│  │  │  • Enterprise Security                             │ │   │ ← NEW
│  │  └─────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────┬─────────────────────┘   │
└──────────────────────────────────────────────┼─────────────────────────┘
                                             │
┌────────────────────────────────────────────▼─────────────────────────┐
│                    Equipment Layer                                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────┐   │
│  │ Tecan Fluent    │  │ Advanced Virtual│  │ Multi-vendor        │   │
│  │ + Full SiLA2    │  │ Lab Environment │  │ SiLA2 Equipment     │   │
│  │ Integration     │  │ (Production)    │  │ Ecosystem           │   │
│  └─────────────────┘  └─────────────────┘  └─────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**最終機能**:
- 完全統合システム
- 高度な観測可能性
- ワークフロー オーケストレーション
- 自動フェイルオーバー
- マルチベンダー対応
- エンタープライズセキュリティ

---

## 🔧 技術スタック進化

### Phase 1 → Phase 5

| コンポーネント | Phase 1 | Phase 2 | Phase 3 | Phase 4 | Phase 5 |
|---------------|---------|---------|---------|---------|----------|
| **Agent Framework** | MCP Basic | AgentCore Gateway | Strands | Strands | Strands Enhanced |
| **Protocol Layer** | Simulated | Gateway API | SiLA2 Protocol | SiLA2 Full | SiLA2 Advanced |
| **Device Layer** | Mock Tools | Gateway Lambda | Virtual SiLA2 | Real + Virtual | Multi-vendor |
| **Infrastructure** | Lambda + ECR | + API Gateway | + SiLA2 Server | + Device Mgmt | + Observability |
| **Deployment** | CloudFormation | + AgentCore | + Docker Compose | + Multi-env | + CI/CD |

---

## 📊 アーキテクチャ品質指標

### 拡張性 (Scalability)
- **Phase 1**: MCP基盤構築
- **Phase 2**: AgentCore Gateway統合
- **Phase 3**: SiLA2プロトコル実装
- **Phase 4**: 実機 + 仮想デバイス対応
- **Phase 5**: エンタープライズスケール対応

### 可用性 (Availability)
- **Phase 1**: MCPサーバー基本可用性
- **Phase 2**: AgentCore Runtime安定性
- **Phase 3**: SiLA2プロトコル信頼性
- **Phase 4**: デバイスフェイルオーバー
- **Phase 5**: 完全な高可用性

### 保守性 (Maintainability)
- **Phase 1**: MCPツールモジュラー設計
- **Phase 2**: Gatewayレイヤー分離
- **Phase 3**: SiLA2プロトコル標準化
- **Phase 4**: プラグイン機構
- **Phase 5**: 完全な疎結合

---

**最終更新**: 2025-01-XX  
**次回レビュー**: Phase完了時